import React, { useState } from 'react';
import {
    Box,
    Button,
    Container,
    TextField,
    Typography,
    ThemeProvider,
    Link,
    createTheme,
  } from '@mui/material';

const Index = () => {
    return(
        <Typography variant="h4" component="h1" gutterBottom>
            Hello matherfucker
          </Typography>
    );
}

export default Index;